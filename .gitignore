node_modules

# Output
.output
.vercel
.netlify
.wrangler/
/.svelte-kit
/build
# Regenerated by `wrangler types` in prepare/build/check; its contents depend on
# whether the build output exists, so committing it only produces churn.
/worker-configuration.d.ts

# OS
.DS_Store
Thumbs.db

# Secrets
.dev.vars
!.dev.vars.example
.env
.env.*

# Media: originals and transcodes live in R2, never in git. Text stays.
content/**/media

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*
# Playwright
test-results
playwright-report
